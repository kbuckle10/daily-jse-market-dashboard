(() => {
  const DATA=window.JSE_DASHBOARD_DATA;
  if(!DATA?.stocks)return;
  const byTicker=new Map(DATA.stocks.map(s=>[String(s.ticker).toUpperCase(),s]));
  const num=v=>v==null||!Number.isFinite(Number(v))?null:Number(v);
  const fmt=v=>v==null?'N/A':`${v.toFixed(2)}×`;
  const nfmt=v=>v==null?'N/A':Number(v).toLocaleString('en-US',{maximumFractionDigits:2});
  function coverage(s){const eps=num(s.epsTtm),dps=num(s.ttmDps),fcfPayout=num(s.fcfPayoutRatio);const earnings=eps!=null&&eps>0&&dps!=null&&dps>0?eps/dps:null;const fcf=fcfPayout!=null&&fcfPayout>0?100/fcfPayout:null;return{eps,dps,fcfPayout,earnings,fcf};}
  function label(v){return v==null?'N/A':v>=2?'Strong':v>=1.5?'Healthy':v>=1.2?'Moderate':v>=1?'Tight':'Weak';}
  function tone(v){return v==null?'neutral':v>=1.5?'positive':v>=1?'amber':'negative';}
  function detail(c){const e=c.earnings==null?'Earnings coverage unavailable':`EPS TTM ${nfmt(c.eps)} ÷ TTM DPS ${nfmt(c.dps)} = ${fmt(c.earnings)}`;const f=c.fcf==null?'FCF coverage unavailable':`FCF payout ${nfmt(c.fcfPayout)}% → 100 ÷ ${nfmt(c.fcfPayout)} = ${fmt(c.fcf)}`;return `${e} • ${f}`;}
  function ensureStyles(){if(document.getElementById('dividendCoverageStyles'))return;const st=document.createElement('style');st.id='dividendCoverageStyles';st.textContent=`.coverage-block{margin-top:7px}.coverage-summary{display:flex;align-items:center;gap:6px;flex-wrap:wrap;padding:7px 8px;border:1px solid var(--border);border-radius:9px;background:var(--surface2);cursor:help}.coverage-summary>span:first-child{font-size:.58rem;color:var(--muted);font-weight:700}.coverage-chip{font-size:.66rem;font-weight:800;padding:3px 6px;border-radius:7px;background:var(--surface)}.coverage-detail{display:none;margin:5px 2px 0;font-size:.55rem!important;color:var(--muted);line-height:1.4}.coverage-block.open .coverage-detail{display:block}.coverage-table-cell{white-space:nowrap;cursor:help}.coverage-table-cell strong{display:block;font-size:.76rem}.coverage-table-cell small{display:block;font-size:.52rem}`;document.head.appendChild(st);}
  function ensureHeader(){const head=document.querySelector('#tableView thead tr');if(!head)return;const labels=[...head.children].map(th=>th.textContent.trim());if(labels.includes('Coverage'))return;let yieldIndex=labels.indexOf('Current Yield');if(yieldIndex<0)yieldIndex=labels.indexOf('Yield');if(yieldIndex<0)return;const th=document.createElement('th');th.textContent='Coverage';head.children[yieldIndex].insertAdjacentElement('afterend',th);}
  function relabelTile(){const tile=document.querySelector('.filter-tile[data-filter="dividend"]');if(!tile)return;const a=tile.querySelector('span'),b=tile.querySelector('small');if(a)a.textContent='Upcoming payments';if(b)b.textContent='Pay date still ahead';}
  function tickerFromCard(card){return card.querySelector('h3')?.textContent?.trim().toUpperCase()||'';}
  function block(s){const c=coverage(s);return `<div class="coverage-block" data-dividend-coverage><div class="coverage-summary" role="button" tabindex="0" aria-expanded="false" title="${detail(c)}"><span>Dividend coverage</span><span class="coverage-chip ${tone(c.earnings)}">Earnings ${fmt(c.earnings)} • ${label(c.earnings)}</span><span class="coverage-chip ${tone(c.fcf)}">FCF ${fmt(c.fcf)} • ${label(c.fcf)}</span></div><p class="coverage-detail">${detail(c)}. Green = healthy/strong, amber = moderate/tight, red = weak (&lt;1×), neutral = N/A because the required input is unavailable.</p></div>`;}
  function decorateCards(){document.querySelectorAll('.stock-card').forEach(card=>{const s=byTicker.get(tickerFromCard(card));if(!s)return;card.querySelector('[data-dividend-coverage]')?.remove();const target=[...card.querySelectorAll('.metric-row')].find(x=>/(?:current|trailing) yield/i.test(x.textContent));if(target)target.insertAdjacentHTML('afterend',block(s));});}
  function decorateTable(){ensureHeader();const head=document.querySelector('#tableView thead tr');if(!head)return;const coverageIndex=[...head.children].findIndex(th=>th.textContent.trim()==='Coverage');if(coverageIndex<0)return;document.querySelectorAll('#stockTableBody tr').forEach(row=>{const ticker=row.querySelector('.ticker')?.textContent?.trim().toUpperCase(),s=byTicker.get(ticker);if(!s)return;const cell=row.children[coverageIndex];if(!cell||!cell.matches('[data-coverage-cell]'))return;cell.title=detail(coverage(s));});}
  function decorateFramework(){const driver=[...document.querySelectorAll('.score-driver')].find(x=>x.querySelector('strong')?.textContent.trim()==='Dividend');if(!driver)return;const metrics=driver.querySelector('.score-driver-metrics');if(metrics&&!metrics.querySelector('[data-coverage-metric]'))metrics.insertAdjacentHTML('beforeend','<span data-coverage-metric>Dividend coverage</span>');const p=driver.querySelector('p');if(p)p.textContent='Dividend yield is judged together with safety, not on yield alone. Earnings coverage (EPS TTM ÷ TTM DPS) and FCF coverage (100 ÷ FCF payout %) show whether earnings and cash flow support the dividend. Strong/healthy coverage supports the income case; tight coverage warrants caution; below 1× is weak. N/A means the required input is unavailable, not that coverage is weak.';}
  function toggle(e){const summary=e.target.closest?.('.coverage-summary');if(!summary)return;if(e.type==='keydown'&&!['Enter',' '].includes(e.key))return;if(e.type==='keydown')e.preventDefault();const b=summary.closest('.coverage-block'),open=!b.classList.contains('open');b.classList.toggle('open',open);summary.setAttribute('aria-expanded',String(open));}
  document.addEventListener('click',toggle);document.addEventListener('keydown',toggle);
  let pending=false;function apply(){if(pending)return;pending=true;requestAnimationFrame(()=>{pending=false;ensureHeader();relabelTile();decorateCards();decorateTable();decorateFramework();});}
  ensureStyles();apply();window.addEventListener('load',apply);
  const tbody=document.getElementById('stockTableBody'),cards=document.getElementById('cardView');
  if(tbody)new MutationObserver(apply).observe(tbody,{childList:true});
  if(cards)new MutationObserver(apply).observe(cards,{childList:true});
  window.JSE_DIVIDEND_COVERAGE={coverage,label};
})();